---
title: Directory Structure
description: The directory structure of SUI.
---

After following the Quick Start Guide, your project root should contain three directories: `data/templates/default`, `suis`, and `public`.

**📂 suis**

The SUI Template Engine acts as a widget to automate web page creation. A Yao application can have multiple SUI widgets, each with multiple templates. Each widget specifies a template root path and target directory for various scenarios.

```bash
├── suis
│   └── web.sui.yao  // SUI widget file defining the template root path, target directory, and template directory.
```

```json
{
  "name": "Yao Demo App", // Widget name
  "storage": { "driver": "local", "option": { "root": "/templates" } }, // Root path relative to data directory
  "public": {
    "root": "/", // Target directory relative to public directory
    "matcher": "[0-9a-zA-Z_\\-\\.]+",
    "remote": "/",
    "index": "/index"
  }
}
```

**📂 data/templates/default**

This directory contains the template source files for the web pages.

```bash
├── data
│   └── templates
│       └── default  # Template directory
│           ├── __assets  # Assets
│           ├── __data.json  # Global dynamic data
│           ├── __document.html  # Document template
│           ├── index  # Home page
│           │   └── index.html
```

**📂 public**

This is the web server's root directory where built files are placed. **No manual action is required.** SUI automatically syncs files here when you run the `sui build` command.

```bash
├── public
│   ├── assets
│   │   ├── libsui.min.js
│   │   ├── libsui.min.js.map
│   ├── index.cfg
│   ├── index.sui
```
